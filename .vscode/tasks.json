{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Client (Vue)",
      "type": "shell",
      "command": "npm run serve",
      "options": {
        "cwd": "${workspaceFolder}/apps/client"
      },
      "problemMatcher": []
    },
    {
      "label": "API (Nest)",
      "type": "shell",
      "command": "npm run start:dev",
      "options": {
        "cwd": "${workspaceFolder}/apps/api"
      },
      "problemMatcher": []
    },
    {
      "label": "AI-Core (FastAPI)",
      "type": "shell",
      "command": "uvicorn app:app --reload --host 0.0.0.0 --port 8000",
      "options": {
        "cwd": "${workspaceFolder}/apps/ai-core"
      },
      "problemMatcher": []
    },
    {
      "label": "Run All Services",
      "dependsOn": [
        "Client (Vue)",
        "API (Nest)",
        "AI-Core (FastAPI)"
      ],
      "dependsOrder": "parallel",
      "problemMatcher": []
    }
  ]
}